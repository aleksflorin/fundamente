#include <stdio.h>
    int main(){
    int a,l=12,z=365,o=8760,i;
        printf("Introdu numarul de ani: ");
        scanf("%d",&a);

        l=a*l;
        z=a*z;
        o=a*o;

        printf("In %d ani avem %d luni, %d zile, %d ore.",a,l,z,o);
        return 0;






    }
